"use strict";
import Cell from './Cell';

class WallCell extends Cell {
    /**
     * @param {*} row 
     * @param {*} col 
     */
    constructor(row, col) {
        super(row, col);
    }
}
export default WallCell;